<project name="Sample usage of Salesforce Ant tasks" basedir="." xmlns:sf="antlib:task" default="deploy">

    <property prefix="sfdc" file="build.properties"/>
	
	<property name="typesets">
		CustomLabels,CallCenter,StaticResource,ApexPage,Queue,Role,Group,CustomObject,Layout,Document,CustomTab,
		CustomApplication,EmailTemplate,Workflow,ApexClass,ApexTrigger,AppMenu,Profile,PermissionSet,
		AccountSharingRules,CampaignSharingRules,CaseSharingRules,CustomObjectSharingRules,Settings,ApprovalProcess,
		CustomPageWebLink,HomePageComponent,HomePageLayout,RemoteSiteSetting
	</property>
	
	<presetdef name="deploy">
		<sf:deploy
				debug="${sfdc.debug}" dryRun="false"
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" maxPoll="${sfdc.maxPoll}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" deployRoot="src/main/metadata"
				transformationsRoot="src/main/transformations" checksums="timestamps-updated.log"/>
	</presetdef>
	
	<presetdef name="loadData">
		<sf:loadData
				debug="${sfdc.debug}"
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}"
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" 
				dataRoot="src/main/resources/data" logDir="logs" timestamps="timestamps-data.log"/>
	</presetdef>
	
	<target name="init">
		<!-- nothing to init for now -->
	</target>
	
	<target name="deployAll">
		<delete file="timestamps-updated.log" quiet="true"/>
		<antcall target="deploy">
			<param name="skip-checksum-retrieval" value="true"/>
		</antcall>
	</target>
	
	<target name="retrieveChecksums" unless="skip-checksum-retrieval">
		<sf:retrieveChecksums sfdcName="${sfdc.checksums}"
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" maxPoll="${sfdc.maxPoll}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" checksums="timestamps-updated.log"/>
	</target>
	
	<target name="deploy" depends="init, retrieveChecksums, destructiveChanges, deploy01, deploy05, deploy07, deploy09, deploy10, deploy12, deploy15, deploy20, deploy21, deploy30, deploy35, deploy40">
		<!-- TODO consider only deployed entities -->
		<sf:retrieveUpdatestamps
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" timestamps="timestamps-deployed.log">
			<sf:typesets names="${typesets}"/>
		</sf:retrieveUpdatestamps>
		<sf:deployChecksums sfdcName="${sfdc.checksums}"
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" maxPoll="${sfdc.maxPoll}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" checksums="timestamps-updated.log"/>
	</target>
	
	<target name="dc">
		<sf:deployChecksums sfdcName="${sfdc.checksums}"
						username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" maxPoll="${sfdc.maxPoll}" 
						useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" checksums="timestamps-updated.log"/>
	</target>
	
	<target name="destructiveChanges">
		<!-- TODO -->
	</target>
	
	<target name="deploy01">
		<deploy>
			<sf:typeset name="labels"/>
			<sf:typeset name="staticresources">
				<!-- the Checksums resource is handled by separate tasks -->
				<sf:exclude name="Checksums"/>
			</sf:typeset>
			<sf:typeset name="remoteSiteSettings"/>
		</deploy>
    </target>
	
	<target name="deploy05">
		<deploy>			
			<sf:typeset name="documents"/>
			<sf:typeset name="callCenters"/>
		</deploy>
    </target>
	
	<target name="deploy07">
		<deploy>
			<sf:typeset name="objects">
				<sf:include name="Picklist_Mapping__c"/>
				<sf:include name="Integration_Picklist__c"/>
				<sf:include name="External_Picklist__c"/>
			</sf:typeset>
		</deploy>
    </target>
	
	<target name="deploy09">
		<deploy>
			<sf:typeset name="objects">
			    <sf:include name="Contact"/>
				<sf:include name="PersonAccount"/>
				<sf:include name="Account"/>
			</sf:typeset>
		</deploy>
    </target>

	<target name="deploy10">
		<deploy>
			<sf:typeset name="objects">
			    <sf:include name=".*__c"/>
				<sf:exclude name="Picklist_Mapping__c"/>
				<sf:exclude name="Integration_Picklist__c"/>
				<sf:exclude name="External_Picklist__c"/>
				<sf:exclude name="SMS_Batch__c"/>
				<sf:exclude name="Retail_Campaign__c"/>
				<sf:exclude name="OB_Task_Handing__c"/>
				<sf:exclude name="Retail_Campaign_Member__c"/>
				<sf:exclude name="Lead__c"/>
				<sf:exclude name="Campaign_Lead__c"/>
				<sf:exclude name="DM_Request__c"/>
			</sf:typeset>
		</deploy>
    </target>
	
	<target name="deploy12">
		<deploy>
			<sf:typeset name="objects">
				<sf:include name="Campaign"/>
			</sf:typeset>
		</deploy>
    </target>
	
	<target name="deploy15">
		<deploy>
			<sf:typeset name="objects">
				<sf:include name="Lead__c"/>
				<sf:include name="Campaign_Lead__c"/>
				<sf:include name="DM_Request__c"/>
				<sf:include name="SMS_Batch__c"/>
				<sf:include name="Retail_Campaign__c"/>
				<sf:include name="OB_Task_Handing__c"/>
				<sf:include name="Retail_Campaign_Member__c"/>
			</sf:typeset>
		</deploy>
    </target>
	
	<target name="deploy20">
		<deploy>
			<sf:typeset name="objects">
				<sf:exclude name="Account"/>
				<sf:exclude name="PersonAccount"/>
				<sf:exclude name="Contact"/>
				<sf:exclude name="Campaign"/>
			    <sf:exclude name=".*__c"/>
			</sf:typeset>
		</deploy>
    </target>
	
	<target name="deploy21">
		<deploy>
			<sf:typeset name="settings">
				<!-- TODO only exclude on Oliver's org
				<sf:exclude name="Address"/-->
			</sf:typeset>
			<sf:typeset name="tabs">
				<sf:exclude name="EPA_.*"/>
				<sf:exclude name="Tasks"/>
				<sf:exclude name="Retail_Campaigns"/>
				<sf:exclude name="BMBS_Campaigns"/>
				<sf:exclude name="Campaign_Calendar"/>
				<sf:exclude name="Retail_Campaign__c"/>	
				<sf:exclude name="Retail_Campaign_Calender"/>					
				<sf:exclude name="Yearly_Target"/>
			</sf:typeset>
			<sf:typeset name="layouts"/>
			<sf:typeset name="email"/>
			<sf:typeset name="groups"/>
			<sf:typeset name="queues"/>
			<sf:typeset name="roles"/>
			<sf:typeset name="workflows"/>
		</deploy>
    </target>
	
	<target name="deploy30">
		<deploy>
			<sf:typeset name="classes"/>
			<sf:typeset name="components"/>
			<sf:typeset name="pages"/>
			<sf:typeset name="tabs">
				<sf:include name="EPA_.*"/>
				<sf:include name="Tasks"/>
				<sf:include name="Retail_Campaigns"/>
				<sf:include name="BMBS_Campaigns"/>
				<sf:include name="Campaign_Calendar"/>
				<sf:include name="Retail_Campaign__c"/>	
				<sf:include name="Retail_Campaign_Calender"/>				
				<sf:include name="Yearly_Target"/>
			</sf:typeset>
			<sf:typeset name="triggers"/>
		</deploy>
    </target>
	
	<target name="deploy35">
		<deploy>
			<sf:typeset name="applications">
				<!-- TODO excluded for now because of deployment errors in metadata API: missing keyboard shortcuts -->
				<sf:exclude name="Service_Cloud_Console_OB"/>
				<sf:exclude name="Serivce_Cloud_Console_IB"/>
				<sf:exclude name="Service_Cloud_Console_SSI"/>
			</sf:typeset>
			<sf:typeset name="permissionsets"/>			
			<sf:typeset name="profiles">
				<!-- TODO FALK these do not deploy on Oliver's org -->
				<!--
				<sf:exclude name="CAC IB CSR"/>
				<sf:exclude name="CAC SSI CSR"/>
				<sf:exclude name="CAC SSI QC"/>
				<sf:exclude name="CAC SSI SV"/>
				<sf:exclude name="CAC SSI TL"/>  
				-->
			</sf:typeset>
			<sf:typeset name="weblinks"/>
			<sf:typeset name="homePageComponents"/>
			<sf:typeset name="homePageLayouts"/>
			<sf:typeset name="appMenus">
				<!-- TODO ecluded for now because retrieve does not include all apps which are necessary for deploy -->
				<sf:exclude name="AppSwitcher"/>
			</sf:typeset>
		</deploy>
	</target>
	
	<target name="deploy40">
		<deploy>
			<sf:typeset name="accountSharingRules"/>
			<sf:typeset name="campaignSharingRules"/>
			<sf:typeset name="caseSharingRules"/>
			<sf:typeset name="customObjectSharingRules"/>
			<sf:typeset name="approvalProcesses"/>
		</deploy>		
    </target>

	<target name="loaddata-oliver">
		<mkdir dir="logs"/>
		<loadData logDir="logs" configId="oliverkoethap1__Environment_Specific_Values__c"/>
	</target>
	
	<target name="loaddata">
		<mkdir dir="logs"/>
		<loadData logDir="logs" configId="Environment_Specific_Values__c"/>
	</target>
	
	<property name="typesets.circle">
		CustomLabels,CallCenter,CustomTab,ApexClass,ApexComponent,ApexPage,ApexTrigger,Group,Queue,Document,AccountSharingRules,CustomPageWebLink,Role,ApprovalProcess
		Workflow,CampaignSharingRules,EmailTemplate,CustomApplication,AppMenu,CaseSharingRules,CustomObjectSharingRules,Settings,HomePageComponent,HomePageLayout,RemoteSiteSetting,StaticResource
	</property>
	
	<!-- TODO
	CustomObject: default objects not included, PersonAccount in retrieve missing
	Profile: check
	PermissionSet: check differences
	-->
	<property name="typesets.not-ready">
		<!-- !!! the following line is just for the PermissionSet !!! -->
		CustomApplication,ApexClass,CustomPermission,ExternalDataSource,CustomObject,ApexPage,CustomTab,PermissionSet,
		Layout,Profile
	</property>
	
	<target name="retrieveAll" depends="init">
		<!-- retrieves all metadata -->
		<sf:retrieve
				debug="${sfdc.debug}" dryRun="false" full="true" cleanupOther="false"
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" maxPoll="${sfdc.maxPoll}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" 
				retrieveRoot="src/main/metadata" transformationsRoot="src/main/transformations" timestamps="timestamps-deployed.log">
			<sf:typesets names="${typesets.circle}"/>
			<sf:feature name="exclude-checksums" param="Checksums"/>
			<!--sf:feature name="include-personaccount"/-->
		</sf:retrieve>
		
		<!-- retrieves all metadata -->
		<sf:retrieve
				debug="${sfdc.debug}" dryRun="false" full="true" cleanupOther="false"
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" maxPoll="${sfdc.maxPoll}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" 
				retrieveRoot="src/main/metadata-retrieve" transformationsRoot="src/main/transformations" timestamps="timestamps-deployed.log">
			<sf:typesets names="${typesets.not-ready}"/>
			<sf:feature name="include-personaccount"/>
		</sf:retrieve>
	</target>

	<target name="retrieve" depends="init">
		<fail message="DEPRECATED: Please use retrieveAll!"/>
		
		<!-- retrieves only changed metadata -->
		<sf:retrieve
				debug="${sfdc.debug}" dryRun="false" cleanupOther="false"
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" maxPoll="${sfdc.maxPoll}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" 
				retrieveRoot="src/main/metadata" transformationsRoot="src/main/transformations" timestamps="timestamps-deployed.log">
			<sf:typesets names="${typesets.circle}"/>
			<sf:feature name="exclude-checksums" param="Checksums"/>
			<!--sf:feature name="include-personaccount"/-->
		</sf:retrieve>
		
		<!-- retrieves only changed metadata -->
		<sf:retrieve
				debug="${sfdc.debug}" dryRun="false" cleanupOther="false"
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" maxPoll="${sfdc.maxPoll}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" 
				retrieveRoot="src/main/metadata-retrieve" transformationsRoot="src/main/transformations" timestamps="timestamps-deployed.log">
			<sf:typesets names="${typesets.not-ready}"/>
			<sf:feature name="include-personaccount"/>
		</sf:retrieve>
	</target>
	
	<!-- Work in Progress -->
	<target name="retrieveUpdatestamps">
		<fail message="DEPRECATED!"/>
		
		<sf:retrieveUpdatestamps
				username="${sfdc.username}" password="${sfdc.password}" serverurl="${sfdc.serverurl}" 
				useProxy="${sfdc.proxy.use}" proxyHost="${sfdc.proxy.host}" proxyPort="${sfdc.proxy.port}" fileName="timestamps-target.log">
			<sf:typesets names="${typesets}"/>
		</sf:retrieveUpdatestamps>
	</target>
	
</project>
