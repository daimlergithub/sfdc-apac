<apex:page Controller="SSINewComplaintController" tabStyle="SSI__c" showHeader="false" sidebar="false">
    <apex:includeScript value="/support/console/27.0/integration.js"/>
    <apex:includeScript value="/soap/ajax/27.0/apex.js"/>
    
    <script type="text/javascript"> 
    	sforce.console.setTabTitle('New Complaint');
    	
        var closeSubtab = function closeSubtab(result) {
            var tabId = result.id;
            sforce.console.closeTab(tabId);
        };
        function doCancel(){
            if (sforce.console.isInConsole()) {
                sforce.console.getEnclosingTabId(closeSubtab);
            }
            else{
                window.close();
            }
        }
    </script>                

    <apex:form id="theForm"> 
        <apex:pageBlock >
            <apex:pageBlockButtons >   
                <apex:commandButton value="New Complaint" action="{!newComplaint}" />                                
                <apex:commandButton value="Cancel" onclick="doCancel(); return false;" />
            </apex:pageBlockButtons>
            <div>
            {!$Request.customerName} Sir/Madam, I understand your feeling, for cars we apologize for any inconvenience to you, I wonder whether you will let me know and I will help you to record in detail,
And will end the call after departments concerned, relevant staff will get in touch with you promptly. 
            <br/>
            您看可以吗？ 
            </div>                
         </apex:pageBlock>      
    </apex:form>
</apex:page>