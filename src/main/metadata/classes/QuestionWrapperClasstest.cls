@isTest
private class QuestionWrapperClasstest {
  
    @isTest
    public static void QuestionWrapperClass(){
        QuestionWrapperClass iqw = new QuestionWrapperClass();
       string recordId;
    string surveyName;
     string questionId;
      String answerId;
      string questionText;
     string questionContext;
     string questionAnswerText;
   string questionType;
     boolean isPicklist;
     boolean isMultiPicklist;
      List<CustomSelectOption> lstSelectOptions;
      List<String> lstSelectedOptions;
    }
}